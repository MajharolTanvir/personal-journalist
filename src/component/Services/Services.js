import useData from "../../hooks/useData";

const Services = () => {
    const [services] = useData('')
    console.log(services);
    return (
        <div>

        </div>
    );
};

export default Services;